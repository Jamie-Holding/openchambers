[tool.ruff]
src = ["src"]
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]
# E/W: pycodestyle, F: pyflakes, I: isort, UP: pyupgrade

[tool.ruff.lint.isort]
known-first-party = ["src", "config"]
